<h1><%= @user.name %></h1>

<dl>
  <dt>Postov</dt>
  <dd><%= @user.posts_count %></dd>
  <dt>Tem</dt>
  <dd><%= @user.topics_count %></dd>
  <dt>IG vrstic</dt>
  <dd><%= @user.messages_count %></dd>
</dl>

<h2>Teme</h2>

<ul>
  <% @topics.each do |topic| %>
    <li><%= link_to topic.title, topic %></li>
  <% end %>
</ul>

<%= paginate @topics %>
