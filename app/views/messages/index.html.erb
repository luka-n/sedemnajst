<%= paginate @messages %>

<table class="table table-striped table-sm">
  <tbody>
    <% @messages.each do |message| %>
      <tr>
        <td><%= link_to message.user.name, message.user %></td>
        <td><%= message.content %></td>
        <td class="text-nowrap"><%= time_ago_in_words message.remote_created_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @messages %>
