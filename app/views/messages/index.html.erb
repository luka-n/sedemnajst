<%= paginate @messages %>

<table class="table table-bordered table-sm table-striped">
  <tbody>
    <% @messages.each do |message| %>
      <tr>
        <td class="align-middle text-nowrap">
          <img src="<%= url_for(message.user.avatar.variant(:tiny)) if message.user.avatar.attached? %>">
          <%= link_to message.user.name, message.user %>
        </td>
        <td class="align-middle"><%= message.content %></td>
        <td class="align-middle text-nowrap"><%= format_time message.remote_created_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @messages %>
